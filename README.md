# seedbox

## Debian 12

### Swizzin
```bash
bash <(curl -sL s5n.sh) && . ~/.bashrc
```
- nginx
- rtorrent (REPO)
- panel
- flood (socket : /var/run/{USER}/.rtorrent.sock)
- filebrowser
- letsencrypt
- jellyfin
